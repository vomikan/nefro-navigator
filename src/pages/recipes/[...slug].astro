---
// src/pages/recipes/[...slug].astro
import fs from 'fs';
import path from 'path';
import matter from 'gray-matter';
import { marked } from 'marked'; // <--- Импортируем marked

const BASE = '/nefro-navigator';

// 1. Функция getStaticPaths (без изменений)
export async function getStaticPaths() {
  const getAllFiles = (dirPath, arrayOfFiles = []) => {
    const files = fs.readdirSync(dirPath);
    files.forEach(file => {
      const filePath = path.join(dirPath, file);
      if (fs.statSync(filePath).isDirectory()) {
        arrayOfFiles = getAllFiles(filePath, arrayOfFiles);
      } else {
        arrayOfFiles.push(filePath);
      }
    });
    return arrayOfFiles;
  };

  const recipesDir = path.join(process.cwd(), 'src/content/recipes');
  if (!fs.existsSync(recipesDir)) return [];

  const allFiles = getAllFiles(recipesDir);
  return allFiles
    .filter(file => file.endsWith('.md'))
    .map(file => {
      const relativePath = path.relative(recipesDir, file);
      const slug = relativePath.replace(/\.md$/, '').replace(/\\/g, '/');
      return { params: { slug }, props: {} };
    });
}

// 2. Получаем параметры
const { slug } = Astro.params;
const filePath = path.join(process.cwd(), 'src/content/recipes', `${slug}.md`);

let data = {};
let htmlContent = ''; // <--- Меняем имя переменной на htmlContent

if (fs.existsSync(filePath)) {
  const fileContent = fs.readFileSync(filePath, 'utf-8');
  const parsed = matter(fileContent);
  data = parsed.data;
  
  // 🔑 КОНВЕРТАЦИЯ MARKDOWN В HTML
  htmlContent = marked.parse(parsed.content); 
} else {
  return Astro.redirect(`${BASE}/recipes/`);
}

const pageTitle = `${data.title || 'Рецепт'} | НефроНавигатор`;
const pageDescription = data.notes || `Рецепт: ${data.title}.`;
---

<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{pageTitle}</title>
  <meta name="description" content={pageDescription} />
  <link rel="icon" type="image/svg+xml" href="/nefro-navigator/favicon.svg" />
  
  <style>
    body { font-family: system-ui, sans-serif; max-width: 800px; margin: 0 auto; padding: 1rem; color: #333; line-height: 1.6; }
    a { color: #C44D34; text-decoration: none; }
    .back { display: inline-block; margin-bottom: 1.5rem; font-weight: 500; }
    h1 { color: #C44D34; border-bottom: 2px solid #e8f5e9; padding-bottom: 0.5rem; }
    
    /* Стили для контента рецепта */
    .recipe-content h2, .recipe-content h3, .recipe-content h4 {
      color: #C44D34;
      margin-top: 1.5rem;
      margin-bottom: 0.5rem;
    }
    .recipe-content p { margin-bottom: 1rem; }
    .recipe-content ul, .recipe-content ol { margin-left: 1.5rem; margin-bottom: 1rem; }
    .recipe-content li { margin-bottom: 0.3rem; }
    .recipe-content img {
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      margin: 1rem 0;
      display: block;
    }
    .recipe-content strong { font-weight: 700; }
    
    .meta-info { background: #f8f9fa; padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem; font-size: 0.9rem; }
    .meta-row { display: flex; justify-content: space-between; border-bottom: 1px solid #eee; padding: 0.3rem 0; }
    .meta-row:last-child { border-bottom: none; }
    
    .source-box { 
      margin-top: 2rem; 
      padding: 1rem 1.5rem; 
      background: #fdf6f0; 
      border-left: 4px solid #5A7D9A; 
      border-radius: 0 8px 8px 0; 
      font-size: 0.95rem; 
      color: #555; 
    }
    
    footer { margin-top: 3rem; padding-top: 1rem; border-top: 1px solid #ddd; font-size: 0.9rem; color: #666; text-align: center; }
  </style>
</head>
<body>

  <a href={`${BASE}/recipes/`} class="back">← К списку рецептов</a>
  
  <article>
    <h1>{data.title}</h1>
    
    <!-- Карточка с мета-данными -->
    <div class="meta-info">
      <div class="meta-row"><span>👨‍🍳 Автор:</span> <strong>{data.author || 'Неизвестно'}</strong></div>
      <div class="meta-row"><span>⏱ Время:</span> <span>{data.prep_time || '-'}</span></div>
      <div class="meta-row"><span>🍽 Порций:</span> <span>{data.servings || '-'}</span></div>
      
      {data.nutrients_per_serving && (
        <div style="margin-top: 0.5rem; padding-top: 0.5rem; border-top: 1px dashed #ccc;">
          <strong>КБЖУ на порцию:</strong><br>
          {data.nutrients_per_serving.kcal} ккал | Б: {data.nutrients_per_serving.protein_g}г | Ж: {data.nutrients_per_serving.fat_g}г | У: {data.nutrients_per_serving.carbs_g}г<br>
          <small style="color:#666">K: {data.nutrients_per_serving.potassium_mg}мг | P: {data.nutrients_per_serving.phosphorus_mg}мг | Na: {data.nutrients_per_serving.sodium_mg}мг</small>
        </div>
      )}
    </div>

    <!-- 🔑 ОСНОВНОЙ КОНТЕНТ (теперь это HTML) -->
    <div class="recipe-content" set:html={htmlContent}></div>

    <!-- БЛОК ИСТОЧНИКА -->
    {data.source_link && (
      <div class="source-box">
        <p style="margin: 0 0 0.5rem 0; font-weight: 600; color: #C44D34;">📚 Первоисточник</p>
        <p style="margin: 0;">
          Этот материал является адаптацией рецепта из канала 
          <a href={data.source_link} target="_blank" rel="noopener" style="color: #C44D34; text-decoration: underline; font-weight: 500;">
            {data.source_name || 'авторского канала'}
          </a>.
          <br>Автор идеи: <strong>{data.author}</strong>. 
          <br><small>Используется с уважением к авторству.</small>
        </p>
      </div>
    )}

  </article>

  <footer>
    <p>© 2026 НефроНавигатор. <a href={`${BASE}/about/`}>О проекте</a></p>
  </footer>

</body>
</html>
